<h1>Editing Donor</h1>

<%= render 'form' %>

<%= link_to 'Delete Donor (cannot be undone!)', @donor, method: :delete, data: { confirm: 'Deleting a donor cannot be undone!  Are you sure?' } %> |
<%= link_to 'Show', @donor %> |
<%= link_to 'Back', donors_path %>
